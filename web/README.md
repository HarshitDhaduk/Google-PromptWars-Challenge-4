# Stadium Copilot - Web

Next.js frontend and BFF for Stadium Copilot. See the [root README](../README.md)
for the full architecture, quickstart, and documentation.

```bash
npm install
npm run dev    # http://localhost:3000 (expects the Python service on :8000)
npm test       # vitest
npm run lint
```
